<!-- display cart only in the right context and if logged in-->
<tal:condition condition="logged_in_user">
  <p class="pull-right">
  	Welcome, <a href="#cart-link">${logged_in_user} </a>! [TODO] Cart here.
  </p>
</tal:condition>

<!-- else display login link if right context but not logged in -->
<tal:condition condition="logged_in_user == ''">
  <p class="pull-right">
  	Welcome! You must <a href="${api.url(api.root, '@@login')}">log in</a>. [TODO] Cart here. 
  </p>
</tal:condition>