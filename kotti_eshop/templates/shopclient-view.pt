<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xml:lang="en"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      metal:use-macro="api.macro('kotti:templates/view/master.pt')">

  <article metal:fill-slot="content"
           class="content"
           i18n:domain="kotti_eshop"
           tal:define="client context; shop client.parent">

    <h1>${client.nickname}</h1>
    <p class="lead">
      ${client.fullname}
    </p>
    <p>
      ${client.shopping_cart}
      ${client.description}
      <!-- test add to cart -->
      ${shop.add_product_to_cart(id_client=9, id_product=8, quantity=1)}
      ${shop.remove_product_from_cart(id_client=9, id_product=4, quantity=1)}
      ${shop.add_product_to_wishlist(id_client=9, id_product=6)}
      <!-- test add to cart -->
    </p>
  </article>
</html>
